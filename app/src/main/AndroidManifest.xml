<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.esha.personalhealthrecord">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="com.androidtutorialpoint.mymapsappsdirection.permission.MAPS_RECEIVE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.InsertBloodPressureActivity" android:label="Insert Blood Pressure"></activity>
        <activity
            android:name=".activities.BloodPressureActivity"
            android:label="Blood Pressure" />
        <activity
            android:name=".activities.PrescriptionRefillActivity"
            android:label="Prescription Refill" />
        <activity
            android:name=".activities.MedicalDetailHistoryActivity"
            android:label="Medical History" />
        <activity android:name=".activities.NewsDetailActivity" />
        <activity android:name=".activities.ReportDetailActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".activities.DashboardActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".activities.ReportActivity"
            android:label="Medical Records" />
        <activity
            android:name=".activities.AlarmActivity"
            android:label="Calendar" />
        <activity
            android:name=".activities.NewsActivity"
            android:label="News" />
        <activity
            android:name=".activities.LocationActivity"
            android:label="Location" />
        <activity
            android:name=".activities.MedicalHistoryActivity"
            android:label="Medical History" />
        <activity android:name=".activities.AddAlarmActivity" />

        <service android:name=".services.MyFirebaseMessagingService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <service android:name=".services.MyFirebaseInstanceIdService">
            <intent-filter>
                <action android:name="com.google.firebase.INSTANCE_ID_EVENT" />
            </intent-filter>
        </service>
        <service
            android:name=".reminder.ReminderAlarmService"
            android:exported="false" />

        <provider
            android:name=".data.RecordProvider"
            android:authorities="com.example.esha.personalhealthrecord.data"
            android:exported="false" />
        <provider
            android:name=".data.pressuredata.BloodPressureProvider"
            android:authorities="com.example.esha.personalhealthrecord.data.pressuredata"
            android:exported="false" />
        <provider
            android:name=".alarmdata.AlarmReminderProvider"
            android:authorities="com.example.esha.personalhealthrecord.alarmdata"
            android:exported="false" />
    </application>

</manifest>